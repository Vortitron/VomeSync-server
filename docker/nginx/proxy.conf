# VomeSync Nginx Proxy Configuration

upstream vomesync_api {
	server vomesync-webserver:3090;
}

upstream vomesync_ws {
	server vomesync-webserver:3001;
}

upstream vomesync_website {
	server vomesync-website:80;
}

# Rate limiting
limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=general:10m rate=30r/s;

# Combined site + API (sync.vome.io)
server {
	listen 80;
	server_name sync.vome.io api.vome.io;
	
	# Security headers
	add_header X-Frame-Options "SAMEORIGIN" always;
	add_header X-XSS-Protection "1; mode=block" always;
	add_header X-Content-Type-Options "nosniff" always;
	add_header Referrer-Policy "no-referrer-when-downgrade" always;
	add_header Content-Security-Policy "default-src 'self' http: https: data: blob: 'unsafe-inline'" always;
	
	# API endpoints
	location /api/ {
		limit_req zone=api burst=20 nodelay;
		
		proxy_pass http://vomesync_api;
		proxy_http_version 1.1;
		proxy_set_header Upgrade $http_upgrade;
		proxy_set_header Connection 'upgrade';
		proxy_set_header Host $host;
		proxy_set_header X-Real-IP $remote_addr;
		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
		proxy_set_header X-Forwarded-Proto $scheme;
		proxy_cache_bypass $http_upgrade;
		
		# Timeouts
		proxy_connect_timeout 30s;
		proxy_send_timeout 30s;
		proxy_read_timeout 30s;
	}
	
	# WebSocket endpoint
	location /ws {
		proxy_pass http://vomesync_ws;
		proxy_http_version 1.1;
		proxy_set_header Upgrade $http_upgrade;
		proxy_set_header Connection "upgrade";
		proxy_set_header Host $host;
		proxy_set_header X-Real-IP $remote_addr;
		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
		proxy_set_header X-Forwarded-Proto $scheme;
		
		# WebSocket timeouts
		proxy_connect_timeout 60s;
		proxy_send_timeout 60s;
		proxy_read_timeout 300s;
	}
	
	# Health check
	location /health {
		proxy_pass http://vomesync_api/api/health;
		access_log off;
	}
		
	# Website (served from vomesync-website container)
	location / {
		limit_req zone=general burst=50 nodelay;
		
		proxy_pass http://vomesync_website;
		proxy_set_header Host $host;
		proxy_set_header X-Real-IP $remote_addr;
		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
		proxy_set_header X-Forwarded-Proto $scheme;
		
		# Static file caching
		location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
			expires 1y;
			add_header Cache-Control "public, immutable";
		}
	}
}

# SSL Configuration (if enabled)
# server {
#     listen 443 ssl http2;
#     server_name sync.vome.io api.vome.io;
#     
#     ssl_certificate /etc/ssl/certs/vomesync.pem;
#     ssl_certificate_key /etc/ssl/private/vomesync.key;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers off;
#     
#     # Same location blocks as HTTP version above
# }

# Redirect HTTP to HTTPS (if SSL enabled)
# server {
#     listen 80;
#     server_name sync.vome.io api.vome.io;
#     return 301 https://$server_name$request_uri;
# }
